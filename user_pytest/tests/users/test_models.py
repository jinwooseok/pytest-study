class TestUnitName:
    def test_method_name(self):
        #arrange -> 데이터 준비 단계 (Given)
        #mock -> mock 객체를 생성하는 단계 (When)
        #act -> 테스트 대상 메서드를 호출하는 단계 (When)
        #assert -> 결과를 검증하는 단계 (Then)
        return 0